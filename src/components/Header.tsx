import React from "react";

const Header = () => {
  return (
    <header className="flex w-full items-center absolute justify-center px-8 h-[85px] bg-transparent top-0">
      <nav>
        <div className="uppercase text-[#fff] font-normal tracking-[7px] text-lg">Galleria</div>
      </nav>
    </header>
  );
};

export default Header;
